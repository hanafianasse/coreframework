spring.application.name=core_framework

# DATABASE
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/${spring.datasource.db-name}
spring.datasource.db-name=my_database
spring.datasource.username=user
spring.datasource.password=user_password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.connection-test-query=SELECT 1

# JPA
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Liquibase
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.yaml
spring.liquibase.enabled=true

# JWT secret key
jwt.secret.key=zE6xz/1DC/pvo/+t2d/RWub/61t+5MH/nH2Xtt6JRqFvC4=/e+zTz/U/8

# Overrides server starting port
server.port = 9090